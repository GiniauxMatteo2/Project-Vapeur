<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Games in {{genre}}</title>
</head>
<body>
    <h1>Games in Genre: {{genre}}</h1>

    {{#if games.length}}
        <ul>
            {{#each games}}
                <li>
                    <strong>{{title}}</strong> - {{description}} (Released: {{releaseDate}})
                </li>
            {{/each}}
        </ul>
    {{else}}
        <p>No games found in this genre.</p>
    {{/if}}

    <a href="/genres">Back to Genres</a>
</body>
</html>
